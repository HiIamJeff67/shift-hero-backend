# Shared Libs

Reusable, low-coupling utility libraries used by the backend template.

## Current Packages

- `ratelimit`: local/distributed rate limiting utilities.
- `responsewriter`: buffered Gin response writer for interception/timeout flows.
- `searchcursor`: cursor encode/decode helpers for paginated search APIs.

Each subdirectory has its own README with API details.
